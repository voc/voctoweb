!!! 5
%html{lang: 'en'}
  %head
    = render 'frontend/shared/header'
    %title
      C3TV -
      = @event.try(:title) || @conference.try(:title) || @location

    = yield :head

  %body
    = render 'frontend/shared/navbar'

    .container-fluid
      = yield

    = render 'frontend/shared/footer'

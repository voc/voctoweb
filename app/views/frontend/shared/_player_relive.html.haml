<!-- Relive Player -->
- content_for :javascript do
  = javascript_include_tag 'relive-player'

.relive-player
  .video-wrap{ |
    style: "min-height: 720px; max-width: #{width}px; max-height: #{height}px;", |
    data: relive_data(event, width, height)}

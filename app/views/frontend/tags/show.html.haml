- content_for :title do
  = @tag

- content_for :body_class do
  page-list

.container-fluid
  %h1 Events for tag "#{@tag}"

  .event-previews
    - if @events.present?
      - @events.each do |event|
        = render partial: 'frontend/events/event', locals: { event: event }
